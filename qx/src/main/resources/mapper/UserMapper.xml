<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.yp.module.qx.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="xyz.yp.module.qx.domain.User">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="code" property="code" jdbcType="VARCHAR" />
        <result column="user_name" property="userName" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="salt" property="salt" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="BaseColumnList">
        id,code,user_name,password,salt,create_time,update_time
    </sql>

    <sql id="searchBy">
        <trim prefix="WHERE" prefixOverrides="AND|OR">
            <if test="id != '' and id != null">
                id = #{id}
            </if>
            <if test="code != '' and code != null">
                AND code = #{code}
            </if>
            <if test="userName != '' and userName != null">
                AND user_name = #{userName}
            </if>
        </trim>
    </sql>

    <insert id="insert" parameterType="xyz.yp.module.qx.domain.User" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO qx_user(code,user_name,password,salt,create_time,update_time) VALUES (#{code},#{userName},#{password},#{salt},#{createTime},#{updateTime})
    </insert>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumnList" />
        FROM qx_user
    </select>

    <select id="selectByUserName" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="BaseColumnList" />
        FROM qx_user
        WHERE user_name = #{userName}
    </select>

    <select id="selectByParams" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumnList" />
        FROM qx_user
        <include refid="searchBy" />
    </select>

    <update id="update">
        UPDATE qx_user
        <set>
            <if test="code != null and code != ''">
                code = #{code},
            </if>
            <if test="userName != null and userName != '' ">
                user_name = #{userName},
            </if>
            <if test="password != null and password != '' ">
                password = #{password},
            </if>
            <if test="salt != null and salt != ''">
                salt = #{salt},
            </if>
            <if test="createTime != null and createTime != ''">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null and updateTime != ''">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>